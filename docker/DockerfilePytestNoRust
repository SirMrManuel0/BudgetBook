FROM python:3.13-slim AS final

WORKDIR /app



# Copy other project files if needed
COPY ../.. .
RUN pip install prebuilt/wheel/*.whl
RUN pip install -r requirements.txt

#CMD ["python", "main.py"]

RUN pip install pytest

CMD ["pytest"]
